{
    "id": 12,
	"name": "java8-hosting",
	"description": "java8 hosting (standalone application with embedded server (e.g. tomcat/jetty))",
	"type": "application",
	"ports": [
		{
			"port": 8080,
			"type": "http"
		}
	],
	"variables": [
		{
			"name": "chdir_path",
			"description": "path to chdir to before running the command"
		},
		{
			"name": "command",
			"description": "Command to run (e.g. java -jar test.jar)"
		}
	]
}