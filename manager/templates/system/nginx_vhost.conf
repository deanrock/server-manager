server {
        listen 80;
        server_name {{domain}};

        {{config|safe}}

        error_log /var/log/nginx/{{domain}}-error.log warn;
        access_log /var/log/nginx/{{domain}}-access.log combined;
}
